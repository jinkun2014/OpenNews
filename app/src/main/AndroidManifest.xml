<?xml version="1.0" encoding="utf-8"?>
<manifest package="me.jinkun.opennews"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:name=".base.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".features.home.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".features.home.HomeActivity"/>
        <activity android:name=".features.home.SearchActivity">
        </activity>
        <activity android:name=".features.news.NewsDetailActivity">
        </activity>
        <activity android:name=".features.home.AboutActivity">
        </activity>
    </application>

</manifest>